#!/bin/bash


srv_is_mounted() {
    local _GUEST_HOST=$(alias_get 'guest.host')
    
    if mount | egrep "^$(echo "${_GUEST_HOST}" | sed 's/\./\\\./g'):/srv on.*$" > /dev/null; then
        return 0
    fi
    
    return 1
}
